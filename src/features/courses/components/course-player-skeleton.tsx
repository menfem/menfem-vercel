// ABOUTME: Loading skeleton component for the course player using unified skeleton system
// ABOUTME: Provides placeholder UI while course data is loading with consistent patterns

import { SkeletonPage } from '@/components/ui/skeleton';

export function CoursePlayerSkeleton() {
  return <SkeletonPage variant="player" />;
}