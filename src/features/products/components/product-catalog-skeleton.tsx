// ABOUTME: Loading skeleton component for product catalog page using unified skeleton system
// ABOUTME: Shows placeholder content while products are loading with consistent patterns

import { SkeletonPage } from '@/components/ui/skeleton';

export function ProductCatalogSkeleton() {
  return <SkeletonPage variant="catalog" />;
}